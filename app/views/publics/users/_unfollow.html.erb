<%= form_for(current_user.active_relationships.find_by(follower_id: @user.id), html: { method: :delete }, remote: true) do |f| %>
<div class="follow-btn">
  <%= f.submit "フォロー解除", class:"btn btn-primary" %>
</div>
<% end %>