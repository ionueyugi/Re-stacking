<div class="container">
	<h2>通知</h2>
	<div class="row">
		<div class="col">
			<% if @notifications.exists? %>
			      <%= render @notifications %>
				<% else %>
			    	<h3>通知はありません</h3>
			<% end %>
		</div>
	</div>
</div>
